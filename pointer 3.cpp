#include <iostream>
using namespace std;

int main() {
   int **ptr = NULL; // Pointer to pointer initialized to NULL
   cout << "Value of ptr: " << ptr << endl; // prints the value of ptr (NULL)
    return 0;
}